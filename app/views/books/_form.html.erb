<%= semantic_form_for(@book, :url => [@store,@book]) do |f| %>
    <% if @book.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

          <ul>
            <% @book.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.inputs do %>
        <%= f.input :title %>
        <%= f.input :description %>
        <%= f.input :price %>
        <%= f.input :amount %>
        <%= f.input :store, :collection => Store.all %>
    <% end %>

    <%= f.buttons do %>
        <%= f.commit_button %>
    <% end %>
<% end %>
